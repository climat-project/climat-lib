{
  "definitions": {
    "Node": {
      "title": "Node",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "params": {
          "type": "object",
          "patternProperties": {
            "\\w+": "^(req|opt):(flag|arg)(:\\w)?(?::\\ *(.*))?$"
        },
        "paramDefaults": {
          "type": "object",
          "patternProperties": {
            "\\w+": ".*"
          }
        },
        "action": {
          "type": "string"
        },
        "children": {
          "type": "array",
          "$recursiveRef": "#/definitions/Node"
        }
      },
      "required": [
        "name",
        "action"
      ]
    }
  },
  "$ref": "#/definitions/Node"
}